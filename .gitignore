# Dependencies
node_modules

# Environment variables (nunca commitar segredos)
.env
.env.local
.env.*.local

# Build output
dist
build
*.tsbuildinfo

# Scraped data (gerada em runtime, não versionar)
backend/data/*.csv

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
.idea
*.swp

# FUSE / filesystem artifacts (arquivos temporários de mounts, não versionar)
.fuse_hidden*

# Config local do assistente (settings específicos da máquina)
.claude/
.vercel
.env*
